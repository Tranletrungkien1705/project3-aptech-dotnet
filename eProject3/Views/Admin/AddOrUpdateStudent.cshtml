@model eProject3.Models.Student
@using eProject3.Repository;
@inject IStudentRepository StudentRespository
@{
    ViewData["Title"] = "AddOrUpdateStudent";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    var x = new Student();
    //x = StudentRespository.GetByIdAsync(id);

    if(Model != null)
    {
        x = Model;
        
    }
}
<head>
    <title>Add/Edit Movie</title>
    <style>
        /* Tùy chỉnh giao diện CSS ở đây */
        .form-container {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }

            .form-container h2 {
                margin-bottom: 20px;
            }
    </style>
</head>
<body>
    c=@Model
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 form-container">
                <h2>Add/Edit Movie</h2>
                @if(1+1==2)
                {
                    var id = x.Id;
                    var code = x.Code;
                    var name = x.Name;
                    var email = x.Email;
                    var gender = x.Gender;
                    var dateOfBirth = x.DateOfBirth;
                   
                    <form id="studentForm" method="POST" asp-action="AddOrUpdateStudentAsyn">
                        <input type="number" name="id" value="@id">
                        <div class="form-group">
                        <label for="Code">Code:</label>
                        <input type="text" class="form-control" id="Code" name="Code" value=" @code">
                        </div>
                        <div class="form-group">
                        <label for="Name">Name:</label>
                        <input type="text" class="form-control" id="Name" name="Name" value="@name">
                        </div>
                        <div class="form-group">
                        <label for="Email">Email:</label>
                        <input type="email" class="form-control" id="Email" name="Email" value="@email">
                        </div>
                        <div class="form-group">
                            <label for="gender">Gender:</label>
                            <select id="gender" name="Gender">
                                @foreach (var genderValue in Enum.GetValues(typeof(Gender)))
                                {
                                    <option value="@genderValue">@genderValue</option>
                                }
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="DateOfBirth">DateOfBirth:</label>
                        <input type="datetime" class="form-control" id="DateOfBirth" name="DateOfBirth" value="@dateOfBirth">
                        </div>
                      
                        <button type="submit" class="btn btn-primary btn-block mt-3">Save</button>
                    </form>
                }

            </div>
        </div>
    </div>

    <!-- Link tới các script cần thiết, bao gồm Bootstrap JS và jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        function saveMovie() {
            // Xử lý lưu dữ liệu ở đây
        }
    </script>
</body>