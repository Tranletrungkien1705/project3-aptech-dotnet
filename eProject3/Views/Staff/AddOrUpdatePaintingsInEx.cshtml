@model eProject3.DTO.DetailPaintingInEx
@using eProject3.Models
@using eProject3.DTO;
@using eProject3.Repository;
@inject IPaintingInExRepository paintingInExRepository;
@inject IExhibitionRepository exhibitionRepo;
@{
    ViewData["Title"] = "AddOrUpdateStaffFeedback";
    Layout = "~/Views/Shared/_LayoutStaff.cshtml";
    var x = new DetailPaintingInEx();
    //x = StudentRespository.GetByIdAsync(id);
    var idofEx = TempData["idofEx"];
    TempData.Keep("idofEx");
    if (Model != null)
    {
        x = Model;

    }
}
<head>
    <title>Add/Edit Movie</title>
    <style>
        /* Tùy chỉnh giao diện CSS ở đây */
        .form-container {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }

            .form-container h2 {
                margin-bottom: 20px;
            }
    </style>
</head>
<body>
    c=@Model
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 form-container">
                <h2>Add/Edit Movie</h2>
                @if (1 + 1 == 2)
                {
                    <form id="staffFeedbackForm" method="POST" asp-action="AddOrUpdatePaintingsInExAsyn">
                        <input type="hidden" name="Id">
                        <div class="form-group">
                            <label for="paintingName">Painting's Name:</label>
                            <select class="form-control" id="paintingName" asp-for="PaintingName" asp-items="ViewBag.paintingName"></select>
                            @*<input type="text" class="form-control" id="paintingName" name="PaintingId" value=" @paintingName">*@
                        </div>
                        <div class="form-group">
                            <label for="ExhibitionName">Exhibition:</label>
                            <input type="number" class="form-control" id="ExhibitionName" name="ExhibitionName" value="@idofEx" disabled>
                        </div>
                        <div class="form-group">
                            <label for="price">Price:</label>
                            <input type="number" class="form-control" id="price" name="Price">
                        </div>
                        <div class="form-group">
                            <label for="isSoldOut">Status:</label>
                            <input type="text" class="form-control" id="isSoldOut" name="IsSoldOut">
                        </div>
                        <button type="submit" class="btn btn-primary btn-block mt-3">Save</button>
                    </form>
                }

            </div>
        </div>
    </div>

    <!-- Link tới các script cần thiết, bao gồm Bootstrap JS và jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        function saveMovie() {
            // Xử lý lưu dữ liệu ở đây
        }
    </script>

</body>