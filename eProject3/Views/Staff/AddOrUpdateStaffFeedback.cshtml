@model eProject3.Models.MarkOfPaintInComp
@using eProject3.Models;
@using eProject3.Repository;
@inject IMarkOfPaintInCompRepository markofpaintincompRepository
@{
    ViewData["Title"] = "AddOrUpdateStaffFeedback";
    Layout = "~/Views/Shared/_LayoutStaff.cshtml";
    var x = new MarkOfPaintInComp();
    //x = StudentRespository.GetByIdAsync(id);

    if (Model != null)
    {
        x = Model;

    }
}
<head>
    <title>Add/Edit Movie</title>
    <style>
        /* Tùy chỉnh giao diện CSS ở đây */
        .form-container {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }

            .form-container h2 {
                margin-bottom: 20px;
            }
    </style>
</head>
<body>
    c=@Model
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 form-container">
                <h2>Add/Edit Movie</h2>
                @if (1 + 1 == 2)
                {
                    var id = x.Id;
                    var mark = x.Mark;
                    var feedback = x.Feedback;


                    <form id="staffFeedbackForm" method="POST" asp-action="AddOrUpdateStaffFeedbackAsyn">
                        <input type="number" name="id" value="@id">
                        <div class="form-group">
                            <label for="mark">Mark:</label>
                            <input type="text" class="form-control" id="Mark" name="Mark" value=" @mark">
                        </div>
                        <div class="form-group">
                            <label for="Feedback">Feedback:</label>
                            <input type="text" class="form-control" id="Feedback" name="Feedback" value="@feedback">
                        </div>
                        <button type="submit" class="btn btn-primary btn-block mt-3">Save</button>
                    </form>
                }

            </div>
        </div>
    </div>

    <!-- Link tới các script cần thiết, bao gồm Bootstrap JS và jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        function saveMovie() {
            // Xử lý lưu dữ liệu ở đây
        }
    </script>

</body>